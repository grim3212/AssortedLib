# Entries are reintroduced as each area is ported to 26.2.
# The 1.20.1 list targeted VertexFormat.offsets, ItemOverrides.<init> and
# ModelManager$ReloadState, all removed in the 1.21.4-26.x model pipeline rewrite.
